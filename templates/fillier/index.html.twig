{% extends 'base.html.twig' %}

{% block title %}Fillier index{% endblock %}

{% block body %}
    <h1>Fillier index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for fillier in filliers %}
            <tr>
                <td>{{ fillier.id }}</td>
                <td>{{ fillier.nom }}</td>
                <td>
                    <a href="{{ path('app_fillier_show', {'id': fillier.id}) }}">show</a>
                    <a href="{{ path('app_fillier_edit', {'id': fillier.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_fillier_new') }}">Create new</a>
{% endblock %}
