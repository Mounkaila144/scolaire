{% extends 'base.html.twig' %}

{% block title %}Fillier{% endblock %}

{% block body %}
    <h1>Fillier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fillier.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ fillier.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_fillier_index') }}">back to list</a>

    <a href="{{ path('app_fillier_edit', {'id': fillier.id}) }}">edit</a>

    {{ include('fillier/_delete_form.html.twig') }}
{% endblock %}
